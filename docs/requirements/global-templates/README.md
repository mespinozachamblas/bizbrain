# Global Templates

These files are **templates**, not instructions to blindly load everything globally.

Use them to seed a lightweight global setup for Cursor/Codex. Keep global context generic. Keep project-specific business logic inside the repository.

Included:
- `cursor-rules/`
- `skills/`
